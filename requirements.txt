Flask
```

---

## **Schritt 2: Projekt auf GitHub hochladen**

**2.1** Falls noch nicht geschehen, erstelle ein neues Repository auf [GitHub](https://github.com/).

**2.2** Füge dein Projekt hinzu und pushe es:
```sh
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/DEIN_USERNAME/DEIN_REPO.git
git push -u origin main
```

---

**Sag Bescheid, wenn dein Projekt auf GitHub ist, dann machen wir mit Render weiter!**  
(Schreib einfach „fertig“ oder poste den Repo-Link.)